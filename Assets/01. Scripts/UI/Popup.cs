using UnityEngine;

namespace Project_Lamb.UI.Popups
{
    public abstract class Popup : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
